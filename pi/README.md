# Pi Sych private overlay

This directory is the tracked, secret-free supervisor overlay for the local Pi
Sych package. It contains Pi settings, global supervisor guidance, approved
private skills and style references, and a validated workbench overlay.

Credentials, model stores, sessions, worker state, installed packages, and
runtime artifacts remain ignored or outside Git. This overlay does not copy,
modify, or depend on legacy OpenCode configuration.
