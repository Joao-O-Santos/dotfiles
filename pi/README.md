# Pi Sych private overlay

This tracked, secret-free directory configures the local Pi supervisor. It loads the local Pi Sych package and optional Plannotator, stores personal model profiles and private skills, and keeps literature paths in private configuration.

Credentials, model stores, sessions, worker state, installed packages, and runtime artifacts remain ignored or outside Git. The public Pi Sych package does not depend on this overlay.
