# SPDX-License-Identifier: GPL-2.0-only or GPL-3.0-only
# Run `R -e "license()"` for details
options(scipen = 15)
options(contrasts = c("contr.sum", "contr.poly"))

options(menu.graphics = FALSE)
options("browser" = "/usr/bin/firefox -P unsafest")
