# SPDX-License-Identifier: BSD-3-Clause
# Copyright © 2009, Michael Stapelberg and contributors
# If my (João O. Santos, 2022) changes are ever considered
# copyrightable, I hereby license them under CC0-1.0

# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 5
}

order += "path_exists VPN"
order += "ipv6"
order += "disk /"
order += "wireless _first_"
order += "ethernet _first_"
order += "battery all"
order += "load"
order += "tztime pt"

wireless _first_ {
        format_up = "W: (%quality at %essid) %ip"
        format_down = "W: down"
}

# CHANGED: Report if ipv6 is enabled but don't show ipv6 address
ipv6 {
	format_up = "ipv6: yes"
}

# CHANGED: report if I'm using a ppp VPN
path_exists VPN {
	# if path exists a ppp VPN is up.
	path = "/proc/sys/net/ipv4/conf/ppp0"
}

ethernet _first_ {
        # if you use %speed, i3status requires root privileges
        format_up = "E: %ip (%speed)"
        format_down = "E: down"
}

battery all {
        format = "%status %percentage %remaining"
}

# CHANGED: display time in my timezone
tztime pt {
        format = "%Y-%m-%d %H:%M:%S"
        timezone = "Portugal"
}

load {
        format = "%1min"
}

disk "/" {
        format = "%avail"
}
